import CatalogView from '@/components/catalog/CatalogView';

export const metadata = { title: 'Рабочие — Подряд PRO' };

export default function LaborCatalogPage() {
  return <CatalogView type="labor" />;
}
