---
phase: quick-260402-w4s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pwa/src/app/page.tsx
autonomous: false
requirements: [LANDING-REBRAND]

must_haves:
  truths:
    - "Zero occurrences of #f5a623, orange, or gold color in page.tsx"
    - "Hero background uses gradient from #1E2A5A to #2F5BFF — no background image"
    - "All headings render in Manrope (font-heading class), body in Inter (font-sans)"
    - "Navbar has logo from /public/logo.png via next/image + 'Подряд PRO' text + blue CTA button"
    - "Three service cards shown: Рабочая сила (hardcoded prices), Аренда техники (fetched), Стройматериалы (fetched)"
    - "'Для исполнителей' section exists with Blue-to-Violet gradient, MAX and Telegram buttons"
    - "Lead form has 152-ФЗ checkbox with /privacy link"
    - "Footer shows ИП Жбанков А.Д. ИНН 550516401202"
  artifacts:
    - path: "pwa/src/app/page.tsx"
      provides: "Complete landing page with brand-book redesign"
      min_lines: 450
      contains: "next/image"
  key_links:
    - from: "pwa/src/app/page.tsx"
      to: "/api/listings/public"
      via: "useFetchListings hook"
      pattern: "fetch.*api/listings/public"
    - from: "pwa/src/app/page.tsx"
      to: "/api/leads"
      via: "handleSubmit POST"
      pattern: "fetch.*api/leads"
---

<objective>
Complete rewrite of pwa/src/app/page.tsx with strict brand-book compliance: blue palette (#1E2A5A, #2F5BFF, #4DA3FF, #6C5CE7), Inter/Manrope fonts, fintech/SaaS visual style. Replace all orange/gold with blue. Add missing "Для исполнителей" section. Use next/image for logo in navbar.

Purpose: Align landing page with fintech brand identity — clean, systematic, professional. Eliminate all legacy orange/gold colors.
Output: Single rewritten file pwa/src/app/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@pwa/src/app/page.tsx
@pwa/src/app/layout.tsx
@pwa/src/app/globals.css
@pwa/tailwind.config.js

<interfaces>
<!-- Tailwind tokens already available (from tailwind.config.js): -->
brand-500: #2F5BFF (primary blue)
brand-900: #1E2A5A (dark blue)
violet-DEFAULT: #6C5CE7
surface: #F7F9FC
font-sans: var(--font-inter) (body)
font-heading: var(--font-manrope) (headings)
shadow-card, shadow-card-hover, shadow-elevated, shadow-glow (all defined)
Animations: fade-in, slide-up, scale-in (all defined)
CSS classes: reveal/visible, noise-overlay, hero-pattern, glass, badge-brand-hero, card-premium, text-gradient, btn-animated-gradient, focus-ring

<!-- Layout.tsx confirms: -->
Inter loaded as --font-inter (subsets: latin, cyrillic)
Manrope loaded as --font-manrope (subsets: latin, cyrillic)
StickyHeader component renders (returns null on "/" path — no conflict)
Body class: font-sans (Inter by default)

<!-- globals.css confirms: -->
h1-h6: font-family var(--font-manrope) automatically
.reveal + .visible for scroll-reveal
prefers-reduced-motion handled

<!-- Existing hooks to KEEP (all work correctly): -->
useFetchListings(type: string) -> Listing[]
useReveal() -> ref (IntersectionObserver, adds .visible class)
useCountUp(target, duration?) -> { value, ref }

<!-- Existing form state to KEEP: -->
category, description, phone, city, messenger, consent, submitted, loading
handleSubmit -> POST /api/leads

<!-- API contract: -->
GET /api/listings/public?type={type} -> { listings: Listing[] }
POST /api/leads -> { phone, work_type, city, comment, source }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite page.tsx — full brand-book redesign</name>
  <files>pwa/src/app/page.tsx</files>
  <action>
Rewrite the entire pwa/src/app/page.tsx file. Keep the 'use client' directive, all existing hooks (useFetchListings, useReveal, useCountUp), type definitions (WorkType, City, Messenger, Listing), constants (CATEGORY_LABELS, CITY_LABELS), form state, and handleSubmit logic EXACTLY as they are. Rewrite ONLY the JSX return and any color references.

**Import to add at top:**
```
import Image from 'next/image';
```

**NAVBAR:**
- Sticky, white bg, shadow-sm, border-b border-gray-100, z-50
- Left side: `<Image src="/logo.png" alt="Подряд PRO" width={36} height={36} />` + `<span className="text-lg font-extrabold text-brand-900 font-heading">Подряд PRO</span>` in a flex row with gap-2 items-center
- Right side: `<a href="#lead-form">Оставить заявку</a>` styled as `bg-brand-500 hover:bg-[#4DA3FF] text-white font-bold text-sm px-5 py-2.5 rounded-[10px] transition-colors cursor-pointer`
- Max-width container: max-w-6xl mx-auto px-4 h-16

**HERO section:**
- `bg-gradient-to-br from-brand-900 to-brand-500` — NO background image, gradient ONLY
- Add classes: `noise-overlay hero-pattern relative overflow-hidden py-24 px-4`
- `<h1>` white, text-3xl sm:text-4xl md:text-5xl, font-extrabold, font-heading
- Main text: "Рабочие · Техника · Стройматериалы — всё для стройки в Омске и Новосибирске" (ALL white, NO orange span — remove the orange span entirely, make the whole heading white)
- Subtitle `<p>`: "Безопасная оплата через эскроу · Оставьте заявку — перезвоним" — text-white/80 text-lg
- CTA button: `bg-white text-brand-500 hover:bg-brand-50 font-bold text-lg px-10 py-4 rounded-[10px] transition-all hover:shadow-glow-lg cursor-pointer`
- Counters: keep useCountUp refs, white text. Stats card: `bg-white/10 backdrop-blur-xl border border-white/20 rounded-2xl`
- All animate-fade-in classes preserved

**SERVICES section (3 cards):**
- Section bg: `bg-surface py-20 px-4`
- Use `<h2 className="font-heading ...">` for "Что мы предлагаем"
- Three cards in grid, each: `bg-white rounded-xl p-8 shadow-card border border-gray-100 transition-all duration-300 hover:shadow-card-hover hover:scale-[1.02]`
- Card 1 (Рабочая сила): icon bg `bg-brand-500/10`, icon stroke `text-brand-500`. Hardcoded prices (Грузчики от 350, Разнорабочие от 300, Уборка территории от 250, Строители от 500). Bottom text `text-brand-500`.
- Card 2 (Аренда техники): icon bg `bg-brand-500/10`, icon stroke `text-brand-500` (NOT orange). Equipment from useFetchListings. Bottom text `text-brand-500`.
- Card 3 (Стройматериалы): icon bg `bg-violet/10`, icon stroke `text-violet`. Materials from useFetchListings. Bottom text `text-violet`.
- Add `useReveal()` ref on container

**БЕЗОПАСНАЯ СДЕЛКА section (4 steps):**
- Section bg: `bg-brand-900 py-20 px-4` (dark blue, NOT #0f1129)
- `<h2>` white, font-heading
- 4 step circles: `bg-gradient-to-br from-brand-500 to-violet` (NOT orange gradient). Shadow: `shadow-[0_4px_20px_rgba(47,91,255,0.3)]`
- Step titles and descriptions white
- Add `useReveal()` ref on container

**ДЛЯ ИСПОЛНИТЕЛЕЙ section (NEW — does not exist yet):**
- Section: `bg-gradient-to-br from-brand-500 to-violet py-20 px-4`
- `<h2>` white, font-heading: "Для исполнителей"
- `<p>` white/90: "Платформа бесплатна для исполнителей. Получайте заказы, выставляйте свои услуги — без комиссий и подписок."
- Two buttons side by side (flex gap-4, center):
  - MAX button: `bg-white text-brand-500 hover:bg-brand-50 font-bold px-8 py-3.5 rounded-[10px] transition-all cursor-pointer` — text "Написать в MAX"
  - Telegram button: `bg-white/20 backdrop-blur border border-white/30 text-white hover:bg-white/30 font-bold px-8 py-3.5 rounded-[10px] transition-all cursor-pointer` — text "Telegram"
- Add `useReveal()` ref (create new one: `const revExecutors = useReveal();`)

**LEAD FORM section:**
- Section bg: `bg-surface py-20 px-4`, id="lead-form"
- Form container: `bg-white rounded-xl p-8 shadow-2xl border border-gray-100`
- All form buttons active state: `bg-brand-500 text-white border-brand-500 shadow-glow` (replace all #2d35a8 with brand-500)
- All focus rings: `focus:border-brand-500 focus:ring-2 focus:ring-brand-500/20`
- Checkbox text-brand-500, link to /privacy
- Submit button: `bg-brand-500 hover:bg-[#4DA3FF] text-white font-bold rounded-[10px]` (NOT orange)
- Hover shadow: `hover:shadow-[0_8px_30px_rgba(47,91,255,0.35)]`
- Keep all form logic exactly as is

**FOOTER:**
- `bg-brand-900 py-8 px-4 text-center` (use brand-900 NOT #0f1129)
- Add `pb-24 md:pb-8` for mobile sticky CTA clearance
- ИП Жбанков А.Д. · ИНН 550516401202 — text-gray-400
- Privacy link — text-gray-500 hover:text-gray-300

**STICKY MOBILE CTA:**
- Keep fixed bottom bar for mobile
- Button: `bg-brand-500 hover:bg-[#4DA3FF] text-white` (NOT orange)

**COLOR RULES — absolute zero tolerance:**
- ZERO occurrences of: #f5a623, #e09510, orange, gold, amber, #FF6B35, podryad.gold anywhere
- Replace EVERY accent color with brand-500 (#2F5BFF) or #4DA3FF (hover/secondary)
- Dark sections use brand-900 (#1E2A5A)
- Text color: text-[#2B2B2B] or text-gray-900 for dark text

**FONT RULES:**
- Root div: `font-sans` (Inter for body — this is the default from layout.tsx)
- All h1, h2, h3: add `font-heading` class explicitly (Manrope)
- Heading letter-spacing is already -0.02em via globals.css

**BORDER RADIUS:**
- Buttons: `rounded-[10px]`
- Cards: `rounded-xl` (12px)
- Form inputs: `rounded-xl`
  </action>
  <verify>
    <automated>cd C:/Users/HP/Desktop/Подряд_PRO/pwa && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- page.tsx compiles without errors
- Zero occurrences of #f5a623, orange, gold, #e09510, #FF6B35 in the file
- Logo rendered via next/image from /logo.png
- "Для исполнителей" section present with Blue-to-Violet gradient
- All buttons use brand-500 blue, not orange
- Hero is gradient-only (no bg image reference), all text white and readable
- Footer shows ИП Жбанков А.Д. ИНН 550516401202
- Form has 152-ФЗ checkbox with /privacy link
- All existing hooks and form logic preserved unchanged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of landing page rebrand</name>
  <files>pwa/src/app/page.tsx</files>
  <action>Human visually verifies the rebranded landing page</action>
  <what-built>Complete landing page rebrand: blue palette, Inter/Manrope fonts, fintech style, new "Для исполнителей" section, logo via next/image</what-built>
  <how-to-verify>
    1. Run `cd pwa && npm run dev` and open http://localhost:3000
    2. Check navbar: logo image + "Подряд PRO" text + blue "Оставить заявку" button
    3. Check hero: blue gradient background (no image), ALL white text, readable, counters animate
    4. Check services: 3 cards with blue/violet accents — NO orange anywhere
    5. Check "Безопасная сделка": dark blue bg, step circles in blue-violet gradient
    6. Check "Для исполнителей": blue-to-violet gradient section, MAX + Telegram buttons
    7. Check lead form: blue buttons, blue submit, 152-ФЗ checkbox
    8. Check footer: brand-900 background, ИП info
    9. Check mobile (375px): sticky CTA bar is blue, layout is responsive
    10. Ctrl+F for "f5a623" or "orange" — should find ZERO results
  </how-to-verify>
  <verify>Visual inspection by human</verify>
  <done>User confirms all sections look correct, blue palette consistent, no orange remnants</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "f5a623\|orange\|gold\|e09510\|FF6B35" pwa/src/app/page.tsx` returns 0
- `grep -c "next/image" pwa/src/app/page.tsx` returns >= 1
- `grep -c "Для исполнителей" pwa/src/app/page.tsx` returns >= 1
- `grep -c "font-heading" pwa/src/app/page.tsx` returns >= 4
- `grep -c "brand-500\|brand-900\|#2F5BFF\|#1E2A5A" pwa/src/app/page.tsx` returns >= 10
- `cd pwa && npx next build` succeeds
</verification>

<success_criteria>
Landing page renders with consistent blue fintech palette, no orange/gold remnants, logo displays correctly, all sections present including new "Для исполнителей", form works, fonts correct.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-w4s-page-tsx-inter-manrope-fintech/260402-w4s-SUMMARY.md`
</output>
