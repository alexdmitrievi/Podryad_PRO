{
  "name": "tender-parser",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn api.main:app --reload --host 127.0.0.1 --port 8000",
    "build": "python scripts/build_frontend.py",
    "test": "pytest tests/ -v",
    "lint": "ruff check . --ignore E501",
    "format": "ruff format ."
  }
}
